[package]
name = "pypippark"
version = "2.0.0-alpha"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1.0"
nix = { version = "0.29", features = ["fs", "user"] }
whoami = "1.3"
